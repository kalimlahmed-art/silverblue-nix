---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: silverblue-nix
description: Fedora Silverblue 44 with persistent /nix support.

base-image: ghcr.io/ublue-os/silverblue-main
# Keep this pinned to the intended Fedora major version.
image-version: 44

modules:
  # Create /nix in the ostree commit so a bind mount can land there at boot.
  - type: script
    scripts:
      - prepare-nix.sh

  # Ship systemd units for the persistent /nix bind mount.
  - type: files
    files:
      - source: system
        destination: /

  - type: systemd
    system:
      enabled:
        - nix-directory.service
        - nix.mount

  # Sets up the cosign policy/key files used by signed registry images.
  - type: signing
